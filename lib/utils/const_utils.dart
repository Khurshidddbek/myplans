import 'package:flutter/material.dart';

const Color login_color = Color(0xFF00C470);
const Color signup_color = Color(0xFF00C470);

const defaultPadding = 16.0;
const Duration defaultDuration = Duration(milliseconds: 300);